/**
 * Pet Barn slot scene — content host for one pet GLB + thumbnail.
 * Players need not visit; the client loads assets by CID from catalog.json.
 */
export function main() {
  // Intentionally empty: storage-only scene.
}
